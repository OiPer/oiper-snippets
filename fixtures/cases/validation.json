[
  {
    "id": "configuration-must-be-array",
    "config": {},
    "input": "",
    "expected": {
      "kind": "error"
    }
  },
  {
    "id": "when-must-not-be-empty",
    "config": [{ "when": [], "body": "replacement" }],
    "input": "",
    "expected": {
      "kind": "error"
    }
  },
  {
    "id": "literal-must-not-be-blank",
    "config": [
      {
        "when": [{ "value": "   " }],
        "body": "replacement"
      }
    ],
    "input": "",
    "expected": {
      "kind": "error"
    }
  },
  {
    "id": "literal-must-be-unique-case-insensitively",
    "config": [
      {
        "when": [{ "value": "brb" }],
        "body": "first"
      },
      {
        "when": [{ "value": "BRB" }],
        "body": "second"
      }
    ],
    "input": "",
    "expected": {
      "kind": "error"
    }
  },
  {
    "id": "regex-flags-must-be-supported",
    "config": [
      {
        "when": [{ "regex": "brb", "flags": "g" }],
        "body": "replacement"
      }
    ],
    "input": "",
    "expected": {
      "kind": "error"
    }
  }
]
