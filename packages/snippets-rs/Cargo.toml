[package]
name = "oiper-snippets"
version = "0.0.2"
edition = "2024"
description = "A general-purpose library for applying configured snippets to input strings"
license = "MIT"
repository = "https://github.com/OiPer/oiper-snippets"

[lib]
path = "src/lib.rs"

[dependencies]
regress = "0.12.0"
serde_json = "1.0.151"
