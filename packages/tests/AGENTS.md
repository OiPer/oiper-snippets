### Test Guidelines

Tests must be hardcoded, deterministic, unique, and explicit. Write every test
individually; never generate tests with arrays, loops, mapping, or `each`. Very
large tests may reuse simple variables, but never reusable objects or arrays.
